'use strict';

angular.module('main.login', []);

// Angular controller for user profile
angular.module('main.login').controller('LoginCtrl', function
                                          ($scope, $http) {

  window.location = '../app/login';

});
